mixin parent-category(records,index=1)
    each element in records 
        - const tab =Array(index+1).join("-- ")
        option(value=element.id) #{tab}#{element.title}
        if element.childItem
            +parent-category(element.childItem,index+1)